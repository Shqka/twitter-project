if tweets
    each tweet in tweets
        div(class="card-body d-flex flex-column tweet-container")
            div(class="d-flex flex-row align-items-center pb-2")
                img(width="60" height="40" class="pr-3" src=tweet.author.avatar)
                span(class="flex-fill")= tweet.author.username

                if editable && tweet.author._id.toString() === currentUser._id.toString()
                    div(class="d-flex flex-row justify-content-center")
                        a(class="fas fa-edit pr-2 text-success" href="/tweets/edit/" + tweet._id)
                        i(class="fas fa-trash text-danger" tweetid=tweet._id)
            
            div= tweet.content

